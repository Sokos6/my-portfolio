import React from 'react';

const TestComponent = () => {
	return (
		<div className="container" style={{ textAlign: 'center' }}>
			<p>
				Testing the store <strong></strong>
			</p>
			<button className="std-btn primary">Change text</button>
		</div>
	);
};

export default TestComponent;
